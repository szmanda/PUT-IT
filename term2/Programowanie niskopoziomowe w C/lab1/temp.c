#include <stdio.h>
//#include <math.h>

int main(){
    int x, y;
    printf("podaj liczby x y: ");
    scanf("%d %d", &x, &y); // for whatever reason always 0
    if ((x <= 5) || (y - 3 <=0)) {
        printf("Brak rozwiązania ☻");
        return 0;
    }
    float r =
        (
            5
        );
    printf("T = %f", x);
    return 0;
}